services:
  salary-calculator:
    build:
      context: .
      pull: false
    image: salary-calculator
    container_name: salary-calculator
    networks:
      - fintrack-network

networks:
  fintrack-network:
    external: true
    name: fintrack-network
